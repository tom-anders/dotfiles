alias latexmk='latexmk -pdf -pvc'
alias luamk='latexmk -pdf -pvc -lualatex -shell-escape'
alias open='mimeopen'

alias dot='cd ~/.dotfiles && git status'

alias lst='lsa -t'
alias vrc="vim ~/.vimrc"
alias zrc="vim ~/.zshrc"
alias irc="vim ~/.config/i3/config"

#List recently changed files
alias lh='ls -lh --color=always -t | head'

alias noctua='ssh -X -t -A tap@fe.noctua.pc2.uni-paderborn.de ssh -X -t noctua-last tmux a'

if [[ $(hostname) = 'arch-laptop' ]] || [[ $(hostname) = 'tom-linux' ]]; then
    alias ls='ls --color=tty --group-directories-first'
    alias vim='nvim'
    alias paul='cd /home/tom/Documents/PaulFiles && paul -u tap'
    alias update='sudo pacman -Syu'
    alias accio='sudo pacman -Sy'

    alias ssm="ssh -t tap@antares.upb.de ssh tap@stefan-schumacher11.uni-paderborn.de"
    alias ssc="ssh -t -X tap@antares.upb.de ssh -X tap@cmspool06"

    alias fuck='sudo $(fc -ln -1)'

    #When leaving ranger, keep the terminal in the current directory
    alias ranger='ranger --choosedir=$HOME/.rangerdir; LASTDIR=`cat $HOME/.rangerdir`; cd "$LASTDIR"' 
    alias r='ranger'

    alias diff='colordiff'

    #Needs sox installed
    alias guitar='for n in E2 A2 D3 G3 B3 E4;do play -n synth 4 pluck $n repeat 2;done'

    alias univpn='~/Documents/scripts/uni_vpn.sh'

    alias cat="bat --theme=1337" #Cat clone with syntax highlighting
    alias find="fd" #Alternative to find

    alias gnuplot="rlwrap -c gnuplot"

    alias find="fd" #Alternative to find

    unalias zz
fi

# fasd & fzf change directory
zz() {
    local file
    file="$(fasd -Rdl "$1" | fzf -1 -0 --no-sort +m)" && cd "${file}" || return 1
}

alias a='fasd -a'        # any
alias s='fasd -si'       # show / search / select
alias d='fasd -d'        # directory
alias f='fasd -f'        # file
alias sd='fasd -sid'     # interactive directory selection
alias sf='fasd -sif'     # interactive file selection
alias z='fasd_cd -d'     # cd, same functionality as j in autojump
alias j='z'
alias v='f -e nvim' # quick opening files with vim
alias o='a -e xdg-open' # quick opening files with xdg-open

cs() {
    cd $1 && ls 
}

ca() {
    cd $1 && lsa
}

#Pipe into less
pl() {
    "$1" | less
}

tldr() {
    wget -qO- cheat.sh/$1
}

alias chx="chmod +x"

alias mv="mv -i"

if [[ $(hostname) = fe1 ]] || [[ $(hostname) = fe-402-1.local ]] || [[ $(hostname) = fe2 ]] || [[ $(hostname) = ln-0002 ]]; then #Double braces for * to work
    alias lsa="ls -al"
    alias gst="git status"
    alias gl="git pull"
    alias gp="git push"
    alias gcam="git commit -am"
    alias ..="cd .."
    alias ...="cd ..."

    alias w="cd $PC2DATA/hpc-prf-pdsm/tap/ThreeLevelSystems"
    alias o="cd $PC2PFS/HPC-PRF-HDPADI/tap/output/ipopt"

    alias ranger=" /upb/scratch/departments/pc2/groups/HPC-PRF-HDPADI/tap/ranger/ranger.py"

    alias mine="ccsinfo -s --mine --fmt=\"%35N %7P %w %5v%D %60u\" | less"

    q() {
        watch -n 3 "squeue -o '%.10i %.9P %.20j %.8T %.8M %.6D %20R %S'"
    }
fi
